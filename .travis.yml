language: node_js

node_js:
  - "12"

before_install:
  - bash build.sh --version 2.0.0-rc.27
  - export PATH=$HOME/.yarn/bin:$PATH

before_script:
  - yarn run build

cache:
  directories:
    - $HOME/.yarn

script:
  - node_modules/.bin/eslint src/**/*.ts
  - yarn test